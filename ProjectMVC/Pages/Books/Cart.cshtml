@page
@model ProjectMVC.Pages.Books.CartModel
@{
}

@if(Model.myOrder!=null)
{<table class="table table-hover">
    <thead>
        <tr>
            <td>
                Book
            </td>
            <td>

            </td>
            <td>

            </td>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model.myOrder)
        {<tr>
            <td>
                @Html.DisplayFor(modelitem=>item.Title)
            </td>
            <td>
                <a class="btn btn-warning" asp-page="./Details" asp-route-id="@item.BookID">Details</a>
            </td>
        <td>
            <form method="post">
                <input type="number" hidden value="@item.BookID" name="id" />
                <input type="submit" value="Remove" name="action" />
            </form>
        </td>
        </tr>
        }
    </tbody>
</table>
 <form method="post">
     <input type="submit" class="btn btn-success" value="Confirm" name="action" />
     <input type="submit" class="btn btn-success" value="Back" name="action" />
 </form>
}